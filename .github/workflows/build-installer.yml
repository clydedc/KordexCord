name: Build Kordexcord CLI

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Placeholder
        run: |
          echo "Aucun build automatique configuré pour l’instant."
          echo "Le job réussit juste pour ne pas bloquer tes pushes."